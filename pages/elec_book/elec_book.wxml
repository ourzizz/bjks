<view class="page" style="padding:20rpx">
	<view class="title">{{shijuan}}</view>
	<view wx:for="{{timu_list}}" wx:key="{{item.timuid}}">
		<view class="tigan">{{item.tihao}}、{{item.tigan}}</view>
		<view>解析：{{item.jiexi}}</view>
		<view>参考答案：{{item.answer}}</view>
	</view>
</view>